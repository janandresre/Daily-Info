# This is a basic workflow to help you get started with Actions
name: Daily Discord Message
on:
  schedule:
    - cron: '12 7 * * *' # Täglich um 09:00 UTC
  workflow_dispatch: # Erlaubt manuelles Starten zum Testen

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: pip install requests yfinance #requirements.txt kann man erstellen, wenn es zu viel wird
      - name: Run script
        env:
          # Hier wird das GitHub-Secret in eine Umgebungsvariable geladen
          MY_DISCORD: ${{ secrets.MY_DISCORD }}
        run: python daily_info_script.py
